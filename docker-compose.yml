version: '3.8'

services:
  backend:
    build: .
    container_name: financial_advisor_api
    ports:
      - "5000:5000"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - FLASK_ENV=production
